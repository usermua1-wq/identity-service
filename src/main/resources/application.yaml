server:
    port: 8080
    servlet:
      context-path: /identity
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identity_service"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update   # create / create-drop / validate / none / update
    show-sql: true

  jwt:
    signingKey: "32f246e932823105f0707c4a85648d72623d3183ea74944ec205842751af539a"